package com.example.keepnotes.utils

import com.example.keepnotes.domain.model.Note

object DummyData {
    val items: List<Note> =
        listOf(
            Note(
                1,
                "In this code firstly",
                note = "In this code firstly we create data class containing our height and color after that we created data class containing our height and color after that we created composable function RandomColorBox which contains properties of our Box(Grid) like it’s size,shape and color. After that inside the Main Function ",
                category = "Code" // Add a category here
            ),
            Note(
                2,
                "In this code firstly we create ",
                note = "In this code firstly we create data class containing our height and color after that we created data class containing our height and color after that we created composable function RandomColorBox which contains properties of our Box(Grid) like it’s size,shape and color. After that inside the Main Function ",
                category = "Code" // Add a category here
            ),
            Note(
                3,
                "Main Function ",
                note = "In this code firstly we create data class containing our height and color after that we created data class containing our height and color after that we created composable function RandomColorBox which contains properties of our Box(Grid) like it’s size,shape and color. After that inside the Main Function ",
                category = "Code" // Add a category here
            ),
            Note(
                4,
                "we create data class",
                note = "In this code firstly we create data class containing our height and color after that we created data class containing our height and color after that we created composable function RandomColorBox which contains properties of our Box(Grid) like it’s size,shape and color. After that inside the Main Function ",
                category = "Code" // Add a category here
            ),
            Note(
                5,
                "containing our height and color",
                note = "In this code firstly we create data class containing our height and color after that we created data class containing our height and color after that we created composable function RandomColorBox which contains properties of our Box(Grid) like it’s size,shape and color. After that inside the Main Function ",
                category = "Code" // Add a category here
            )
        )
}